# Purpose: Keep frontend Docker build context small.
# Callers: docker build and docker compose.
# Deps: Docker ignore syntax.
# API: ignore patterns for frontend image.
# Side effects: excludes local runtime and cache files.
node_modules
dist
data
.claude
*.tsbuildinfo
